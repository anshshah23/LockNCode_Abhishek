[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[requires]
python_version = "3.10"

[packages]
fastapi = ">=0.100"
uvicorn = ">=0.20"
numpy = ">=1.21"
requests = ">=2.25"
scikit-learn = ">=1.0"
pydantic = ">=1.10"


[scripts]
dev = "uvicorn app:app --host 0.0.0.0 --port 8880 --reload"
